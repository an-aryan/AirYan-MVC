@model BookingViewModel

<h2>All Flights</h2>

<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
            <tr>
                @* <th>Select</th> *@
                <th>Source</th>
                <th>Destination</th>
                <th>Departure Time</th>
                <th>Arrival Time</th>
                <th>Airline</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var flight in Model.AvailableFlights)
            {
                <tr>
                    @* <td>@Html.ActionLink("Book This Flight", "ConfirmationApi", new { fid = flight.FlightId })</td> *@
                    <td>@flight.DepartureAirport?.City</td>
                    <td>@flight.ArrivalAirport?.City</td>
                    <td>@flight.DepartureTime</td>
                    <td>@flight.ArrivalTime</td>
                    <td>@flight.AirlineName</td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Additional fields for AryanBooking -->
@* <label asp-for="BookingDetails.PassengerId">Passenger ID:</label>
<input asp-for="BookingDetails.PassengerId" class="form-control" /> *@
@* <button type="submit" class="btn btn-primary">Book Flight</button> *@
